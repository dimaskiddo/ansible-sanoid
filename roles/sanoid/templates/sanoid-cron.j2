#!/bin/bash

export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

(
 echo "--------------------------------------------------" 
 echo $(date) sanoid start

 echo ""
 {% if ansible_distribution == "Ubuntu" %}run-one{% endif %} sanoid --cron --verbose
 echo ""

 echo $(date) sanoid end
 echo "--------------------------------------------------"
) >> /var/log/sanoid-cron.log 2>&1 &

exit 0
